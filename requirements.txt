gradio==4.32.2
gradio-client==0.15.1
pydantic==2.10.6
ultralytics
pathlib
typing
imageio-ffmpeg
torchvision
pytesseract
transformers
torch
sentencepiece
protobuf==3.20.2
opencv-python
timm
# Enhanced detection dependencies
numpy<2.0.0
pillow
scipy
# K-means color detection dependencies
scikit-learn>=1.3.0
# ResNet-18 feature extraction dependencies
torch>=1.9.0
torchvision>=0.10.0
# PaddleOCR dependencies (GPU enabled for CUDA support)
# paddlepaddle-gpu==2.6.2  # Only use if CUDA is available on server
paddlepaddle==2.6.2
paddleocr>=3.1.0
psycopg2-binary
setuptools==68.0.0
# LightOnOCR integration (GPU ready when CUDA is available)
# Additional GPU dependencies can be added when CUDA is properly configured
# Optional enhanced modules - these are warnings only, core functionality works without them
